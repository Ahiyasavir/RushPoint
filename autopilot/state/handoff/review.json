{
  "verdict": "revise",
  "reasons": [
    "Delivers a real admin-visible product change: new /control ControlRoomPage with four prioritized sections, top-nav entry, and manager default home (roles.tsx ROLE_ROUTES manager[0]='/control'). PRODUCT DELIVERY GATE satisfied.",
    "Data sources are correct and consistent with existing pages: adminAlerts path + !acknowledged filter mirror AlertsBanner; paused/closed stations from public/data/tasks; over-cap mirrors JudgePage (maxDurationMinutes vs arrivedAt); listPendingArrivals({status:'arrived'}) is supported server-side (functions/src/index.ts:622-623).",
    "Conventions respected: callable<>() + usePoll(~12s), PendingArrival imported from @rushpoint/shared, logical RTL classes (ms-auto/text-start), loading/error/'All clear' empty states present.",
    "EN/HE parity verified: new ctrl.* keys added in both dicts; 6 reused keys (common.refresh, alerts.unknownTeam/emergency/technical, mgr.statusClosed/statusPaused) all present in both EN and HE.",
    "Typecheck passes. New file ControlRoomPage.tsx is lint-clean (eslint exit 0); the admin lint failure is pre-existing in CheckInsPage.tsx (no-alert rule), unrelated to this change.",
    "DEFECT (acceptance criterion #3): the Active SOS section — the highest-priority block — routes rows to /manager, but ManagerPage has NO alert/SOS/acknowledge UI (verified: no matches for alert|SOS|acknowledge|adminAlerts in ManagerPage.tsx). The ack feed (AlertsBanner) lives on /checkins, /station, /tene, /volunteer. So an organizer clicking an active SOS lands on a station-control page where they cannot act on it. Stations->/manager is correct; only SOS routing is non-actionable."
  ],
  "requiredFixes": [
    "Route the SOS-alert rows and the SOS section's action button to a page that actually surfaces the AlertsBanner ack UI (e.g. navigate('/checkins')) instead of /manager, so the organizer can acknowledge the alert from the deep-link. Update the section's action label (currently ctrl.openManager) accordingly. Leave stations routing to /manager unchanged (correct there)."
  ],
  "behaviorPreserved": true,
  "riskNotes": "Manager's default landing page changed from /heatmap to /control (sanctioned by task hints) — confirm organizers expect this on event day. Over-cap and pending-review sections only reflect teams already confirmed into the judge 'arrived' queue (mirrors JudgePage scope), not field-station dwell time; this is an honest, documented scope limitation, not a bug. Alerts/stations are read via getDocs in a ~12s poll rather than onSnapshot, so it is near-real-time, not instant."
}
